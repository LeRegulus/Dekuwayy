<form method="post" action="{{ path('admin_anounce_delete', {'slug': anounce.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anounce.slug) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
