<form method="post" action="{{ path('anounce_delete', {'id': anounce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anounce.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
